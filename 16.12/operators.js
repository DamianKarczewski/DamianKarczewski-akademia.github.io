'use strict';

var liczba = 10;

liczba += 5; //liczba = liczba +5;

var liczbaStr = '15';

console.log(liczba === 10);

var warunek = (liczba % 2 == 0) ? 'parzysta' : 'nieparzysta';

console.log(warunek);